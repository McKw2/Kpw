# KPW Build website

Static website for KPW Build, combining landscaping services with KPW Store
product links.

## Preview locally

Run a simple static server from this directory:

```sh
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
