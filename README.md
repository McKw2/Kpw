# KPW

Single-page marketing and portfolio site starter for the KPW brand.

## Run locally

Because the site is plain HTML, CSS, and JavaScript, you can serve it with any
static file server. One easy option is Python:

`python3 -m http.server 4173`

Then open `http://localhost:4173`.
